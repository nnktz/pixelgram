import { Inter } from 'next/font/google'
import local from 'next/font/local'

export const calSans = local({
    src: '../public/fonts/CalSans-SemiBold.woff2'
})

export const inter = Inter({subsets: ['latin']})